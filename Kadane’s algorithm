void func(int a[],int n)
{
    int max=a[0];
    long long sum=0;
    for(int i=0;i<n;i++)
    {   
  
        sum+=a[i];
        
        if(sum>max)
            max=sum;
        if(sum<0)
        sum=0;
    }
    printf("%d",max);
}
